<?php return [

    /*
    |--------------------------------------------------------------------------
    | Class Aliases
    |--------------------------------------------------------------------------
    |
    | This array of class aliases will be listed as possible Sitemap data sources
    | Routes to sitemaps look like this:
    |       /sitemap/<name of the feed>
    | The format of this array is:
    |       <name of the sitemap as defined in the route parameter> => <namespaced class name to instantiate>
    |
    | Example: we want the sitemap at url "sitemap/home", we add a 'home' => MyNamespace\MyClassname::class
    | which will be instantiated by the sitemap controller to display the sitemap xml.
    |
    */
    'aliases'=>[

    ]
];